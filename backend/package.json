{
    "scripts": {
        "setup": "python3 -m venv venv && . venv/bin/activate && pip install flask && pip install flask-cors",
        "windows:setup": "pip install virtualenv && virtualenv venv && venv\\Scripts\\activate && pip install flask && pip install flask-cors",
        "start": "python3 ./src/app.py"
    }
}
