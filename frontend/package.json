{
    "scripts": {
        "start": "python3 -m http.server --bind 127.0.0.1 8000",
        "powershell:start": "powershell python3 -m http.server --bind 127.0.0.1 8000; python -m http.server --bind 127.0.0.1 8000"
    }
}
